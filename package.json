{
  "name": "heylarmah-exchange",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public --project-name heylarmah-exchange",
    "deploy": "npm run deploy:worker && npm run deploy:pages"
  },
  "devDependencies": {
    "supabase": "^2.78.1",
    "wrangler": "^3.0.0"
  }
}
